# Backend API URL (used by frontend to connect to the server)
REACT_APP_API_URL=http://localhost:5000

# Backend server port
PORT=5000

# Base URL for constructing image URLs (optional)
# If not set, the server will automatically construct URLs based on the request
# BASE_URL=http://localhost:5000
